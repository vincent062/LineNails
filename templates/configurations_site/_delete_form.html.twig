<form method="post" action="{{ path('app_configurations_site_delete', {'id': configurations_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ configurations_site.id) }}">
    <button class="btn">Delete</button>
</form>
