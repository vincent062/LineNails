{% extends 'base.html.twig' %}

{% block title %}Accueil - LineNails{% endblock %}

{% block body %}
    <div class="container my-5">
        {# Section de bienvenue (Hero) #}
        <div class="p-5 text-center bg-light rounded-3 mb-5 shadow-sm">
            <h1 class="text-body-emphasis display-4">Bienvenue chez LineNails</h1>
            <p class="col-lg-8 mx-auto fs-5 text-muted">
                L'art de la manucure, où chaque ongle devient une œuvre. Découvrez nos créations et prenez rendez-vous.
            </p>
            <div class="d-inline-flex gap-2 mb-5">
                <a href="{{ path('app_portfolio_index') }}" class="d-inline-flex align-items-center btn btn-primary btn-lg px-4 rounded-pill" type="button">
                    Voir le Portfolio
                </a>
            </div>
        </div>

        {# Section des derniers projets #}
        <h2 class="text-center mb-4">Nos dernières réalisations</h2>
        
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            {% for project in projects %}
                <div class="col">
                    <div class="card shadow-sm h-100">
                        {# Pensez à ajouter une propriété "image" à votre entité Portfolio plus tard #}
                        {# <img src="..." class="card-img-top" alt="{{ project.nom }}"> #}
                        <div class="card-body">
                            <h5 class="card-title">{{ project.nom }}</h5>
                            {% if project.service %}
                                <p class="card-text text-muted">{{ project.service.nom }}</p>
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% else %}
                <p>Aucun projet à afficher pour le moment.</p>
            {% endfor %}
        </div>
    </div>
{% endblock %}
