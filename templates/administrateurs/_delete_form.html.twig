<form method="post" action="{{ path('app_administrateurs_delete', {'id': administrateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administrateur.id) }}">
    <button class="btn">Delete</button>
</form>
