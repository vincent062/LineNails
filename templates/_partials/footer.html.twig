{# templates/_partials/footer.html.twig #}
<footer class="footer mt-auto py-4">
    <div class="container">
        <div class="row gy-4">
            {# Colonne 1: Logo et description #}
            <div class="col-lg-4 text-center text-lg-start">
                <a href="{{ path('app_home') }}">
                    <img src="{{ asset('images/logosansfond.png') }}" alt="LineNails Logo" class="footer-logo mb-2">
                </a>
                <p class="footer-tagline">"L'art au bout de vos doigts."</p>
            </div>

            {# Colonne 2: Liens de navigation #}
            <div class="col-lg-2 col-6">
                <h5>Navigation</h5>
                <ul class="list-unstyled">
                    <li><a href="{{ path('app_home') }}" class="footer-link">Accueil</a></li>
                    <li><a href="{{ path('app_categorie_index') }}" class="footer-link">Prestations</a></li>
                    <li><a href="{{ path('app_portfolio_index') }}" class="footer-link">Portfolio</a></li>
                    <li><a href="#" class="footer-link">Contact</a></li>
                </ul>
            </div>

            {# Colonne 3: Coordonnées (AVEC LA CORRECTION) #}
            <div class="col-lg-3 col-6">
                <h5>Contact</h5>
                <ul class="list-unstyled">
                    {% set config = site_config.getConfig() %}
                    {% if config %}
                        <li><i class="bi bi-geo-alt-fill me-2"></i>{{ config.adressePostale }}</li>
                        <li><i class="bi bi-telephone-fill me-2"></i>{{ config.telephone }}</li>
                        <li><i class="bi bi-envelope-fill me-2"></i>{{ config.emailContact }}</li>
                    {% endif %}
                </ul>
            </div>

            {# Colonne 4: Réseaux Sociaux (AVEC LA CORRECTION) #}
            <div class="col-lg-3 text-center text-lg-end">
                <h5>Suivez-moi</h5>
                <div class="social-icons">
                    {% set config = site_config.getConfig() %}
                    {% if config and config.urlPageFacebook %}
                        <a href="{{ config.urlPageFacebook }}" target="_blank" class="social-icon"><i class="bi bi-facebook"></i></a>
                    {% endif %}
                    {% if config and config.urlPageInstgram %}
                        <a href="{{ config.urlPageInstgram }}" target="_blank" class="social-icon"><i class="bi bi-instagram"></i></a>
                    {% endif %}
                </div>
            </div>
        </div>
        
        <div class="text-center pt-3 mt-3 border-top border-secondary-subtle">
            <small>&copy; {{ "now"|date("Y") }} LineNails - Tous droits réservés.</small>
        </div>
    </div>
</footer>