{% extends 'base.html.twig' %}

{% block title %}CategoriesPortfolio{% endblock %}

{% block body %}
    <h1>CategoriesPortfolio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categories_portfolio.id }}</td>
            </tr>
            <tr>
                <th>Id_categorie_portfolio</th>
                <td>{{ categories_portfolio.idCategoriePortfolio }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categories_portfolio.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categories_portfolio_index') }}">back to list</a>

    <a href="{{ path('app_categories_portfolio_edit', {'id': categories_portfolio.id}) }}">edit</a>

    {{ include('categories_portfolio/_delete_form.html.twig') }}
{% endblock %}
