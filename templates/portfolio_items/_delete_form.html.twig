<form method="post" action="{{ path('app_portfolio_items_delete', {'id': portfolio_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ portfolio_item.id) }}">
    <button class="btn">Delete</button>
</form>
